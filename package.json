{
  "name": "raftlabs-ass-deployment",
  "version": "1.0.0",
  "description": "Root package.json for deploying frontend and backend together",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  }
}
